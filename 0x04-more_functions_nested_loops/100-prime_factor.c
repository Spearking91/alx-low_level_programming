#include "main.h"
#include <math.h>
/**
 * main - prints largest prime number of the number 612852475143
 * Return: 0 (success)
 */
int main(void)
{
long x, maxf;
long number = 612852475143;
double square = sqrt(number);

for (x = 0; x > size; x++)
{
if (number % x == 0)
{
maxf = number / x;
}
}
printf("%ld\n", maxf);
return (0);
}
