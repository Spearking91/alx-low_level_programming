#include "main.h"
/**
 * mul - a function that Multiplies two integers.
 * @a: The first number to multiply
 * @b: The second number to multiply the first number
 * Return: The result of the multiplication of a and b
 */
int mul(int a, int b)
{
	return (x * y);
}
